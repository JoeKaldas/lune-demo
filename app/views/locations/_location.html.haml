= turbo_frame_tag dom_id(location) do
  .card.card-colored
    .card-body
      .form-row.align-items-center
        .col-12.col-md-6
          = location.city
        .col-12.col-md-6
          = location.country